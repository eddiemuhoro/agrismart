// styles.js or styles.ts
import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  container: {
    flex: 1, // Full height for all screens
    paddingVertical: 32, // Apply vertical padding to all screens
    backgroundColor: "#FFF", // You can add more global styles if needed
  },
});
